<Window x:Class="DLVoiceLibrary.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="入力"
        Width="420" SizeToContent="Height" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource {x:Type Window}}">
    <StackPanel Margin="16">
        <TextBlock x:Name="PromptText" Text="名前を入力してください" Margin="0,0,0,8" />
        <TextBox x:Name="InputBox" Padding="6,4" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="キャンセル" Padding="12,6" Margin="0,0,8,0" Click="OnCancelClick" IsCancel="True" />
            <Button Content="OK" Padding="12,6" Click="OnOkClick" IsDefault="True" />
        </StackPanel>
    </StackPanel>
</Window>
